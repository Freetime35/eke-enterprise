# PR-042 — EUR-Lex Full Import Pipeline

```bash
git checkout main
git pull
git checkout -b feature/pr-042-eurlex-full-import
```

Extract the archive, then run:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): enrich EUR-Lex import pipeline
```
