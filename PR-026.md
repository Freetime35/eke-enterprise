# PR-026 — SQLAlchemy Mapping Refinement

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-026-sqlalchemy-mapping-refinement
```

Extract this archive at the repository root.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
refactor(infrastructure): refine SQLAlchemy mappings
```
