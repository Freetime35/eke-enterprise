# PR-027 — Alembic Migrations

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-027-alembic-migrations
```

Extract this archive at the repository root.

## Install

```bash
python -m pip install -e ".[dev]"
```

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(infrastructure): add Alembic migrations
```
