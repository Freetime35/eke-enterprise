# PR-028 — FastAPI Bootstrap

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-028-fastapi-bootstrap
```

Extract this archive at the repository root.

## Install

```bash
python -m pip install -e ".[dev]"
```

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Run

```bash
python -m uvicorn eke.presentation.api.main:app --reload
```

## Commit

```text
feat(presentation): bootstrap FastAPI application
```
