# PR-049 — Import Job Listing & Filtering

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-049-import-job-search
```

Extract the archive at the repository root.

In `tests/presentation/api/test_openapi_contract.py`, add this operation ID:

```text
eurlex_import_jobs_search_import_jobs
```

No new path is required because `GET` shares the existing
`/imports/eurlex/jobs` collection path with `POST`.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(presentation): add import job search
```
