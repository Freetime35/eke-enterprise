# PR-051 — Import Job Retry

```bash
git checkout main
git pull
git checkout -b feature/pr-051-import-job-retry
```

Extract the archive.

In `tests/presentation/api/test_openapi_contract.py`, add:

```text
/imports/eurlex/jobs/{job_uuid}/retry
```

and:

```text
eurlex_import_jobs_retry_import_job
```

Validate:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add import job retry
```
