# PR-044 — Import Job Persistence

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-044-import-job-persistence
```

Extract the archive at the repository root.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(infrastructure): add import job persistence
```
