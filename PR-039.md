# PR-039 — EUR-Lex Metadata Parser

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-039-eurlex-metadata-parser
```

Extract the archive at the repository root.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(infrastructure): parse EUR-Lex RDF metadata
```
