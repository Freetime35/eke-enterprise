# PR-009.1 — GitHub Actions CI

## Files added

- `.github/workflows/ci.yml`
- `docs/prs/PR-009.1-GitHub-Actions.md`

## Apply on branch

```bash
git checkout main
git pull
git checkout -b feature/pr-0091-github-actions
```

## Local validation

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Recommended commit

```text
ci: add GitHub Actions quality workflow
```

## Merge

```bash
git checkout main
git merge --ff-only feature/pr-0091-github-actions
git push origin main
git branch -d feature/pr-0091-github-actions
```
