# PR-043 — Bulk Import API

```bash
git checkout main
git pull
git checkout -b feature/pr-043-bulk-import-api
```

Extract the archive at the repository root.

Update `src/eke/presentation/api/app.py` by importing
`eurlex_bulk_imports_router` and registering it with:

```python
app.include_router(eurlex_bulk_imports_router)
```

Update the OpenAPI contract expected paths with:

```text
/imports/eurlex/bulk
```

Then run:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(presentation): add bulk EUR-Lex import API
```
