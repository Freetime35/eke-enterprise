# PR-045 — Import Job Service

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-045-import-job-service
```

Extract the archive at the repository root.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(application): add import job service
```
