# PR-036 — OpenAPI Contract Hardening

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-036-openapi-contract
```

Extract the archive at the repository root.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(presentation): harden OpenAPI contract
```
