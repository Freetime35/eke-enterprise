# PR-068 — EUR-Lex Legal Lifecycle and Amendment Dates

Extract this archive at the repository root, then run:

```cmd
apply_pr068.cmd
```

Commit:

```text
feat(eurlex): enrich legal lifecycle and amendment dates
```
