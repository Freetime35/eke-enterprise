# PR-084 — EUR-Lex Structured Boolean Logic

Extract this archive at the repository root, replacing the previous
PR-084 files, then run:

```cmd
apply_pr084.cmd
```

Commit:

```text
feat(eurlex): structure explicit Boolean rule logic
```
