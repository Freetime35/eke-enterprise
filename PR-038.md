# PR-038 — EUR-Lex Client Port & HTTP Adapter

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-038-eurlex-http-client
```

Extract the archive at the repository root.

Apply the dependency change described in `pyproject.toml.patch`, then delete the
patch file.

## Validate

```bash
python -m pip install -e ".[dev]"
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(infrastructure): add EUR-Lex HTTP client
```
