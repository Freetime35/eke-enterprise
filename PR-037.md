# PR-037 — CELEX Identifier Parsing & Validation

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-037-celex-identifier
```

Extract the archive at the repository root.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(domain): add CELEX identifier parsing
```
