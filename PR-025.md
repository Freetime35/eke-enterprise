# PR-025 — Unit of Work

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-025-unit-of-work
```

Extract this archive at repository root.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(application): introduce Unit of Work
```
