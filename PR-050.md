# PR-050 — Import Job Cancellation

```bash
git checkout main
git pull
git checkout -b feature/pr-050-import-job-cancellation
```

Extract the archive.

In `tests/presentation/api/test_openapi_contract.py`, add:

```text
/imports/eurlex/jobs/{job_uuid}/cancel
```

and:

```text
eurlex_import_jobs_cancel_import_job
```

Validate:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add import job cancellation
```
