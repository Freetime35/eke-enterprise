# PR-031 — Resource Titles API

```bash
git checkout main
git pull
git checkout -b feature/pr-031-resource-titles-api
```

Extract the archive, then run:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add Resource Titles API
```
