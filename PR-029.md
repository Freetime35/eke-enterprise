# PR-029 — Resource API CRUD

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-029-resource-api
```

Extract this archive at the repository root.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(presentation): add Resource CRUD API
```
