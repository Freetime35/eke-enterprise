# PR-075 — EUR-Lex Legal Definitions Extraction

Extract this archive at the repository root, then run:

```cmd
apply_pr075.cmd
```

Commit:

```text
feat(eurlex): extract explicit English legal definitions
```
