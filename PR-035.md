# PR-035 — Resource Classifications API

```bash
git checkout main
git pull
git checkout -b feature/pr-035-resource-classifications-api
```

Extract the archive at the repository root, then run:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add Resource Classifications API
```
