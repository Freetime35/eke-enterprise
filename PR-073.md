# PR-073 — EUR-Lex Document Structure Extraction

Extract this archive at the repository root, then run:

```cmd
apply_pr073.cmd
```

Commit:

```text
feat(eurlex): extract structured document hierarchy
```
