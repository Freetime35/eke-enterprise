# PR-033 — Resource Relationships API

```bash
git checkout main
git pull
git checkout -b feature/pr-033-resource-relationships-api
```

Extract the archive at the repository root, then run:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add Resource Relationships API
```
