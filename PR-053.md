# PR-053 — Import Job Status Summary

```bash
git checkout main
git pull
git checkout -b feature/pr-053-import-job-summary
```

Extract the archive.

In `tests/presentation/api/test_openapi_contract.py`, add:

```text
/imports/eurlex/jobs/summary
```

and:

```text
eurlex_import_jobs_summarize_import_jobs
```

Validate:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add import job status summary
```
