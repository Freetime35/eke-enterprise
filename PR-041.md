# PR-041 — EUR-Lex Import API

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-041-eurlex-import-api
```

Extract the archive at the repository root.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(presentation): add EUR-Lex import API
```
