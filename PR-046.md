# PR-046 — Import Jobs API

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-046-import-jobs-api
```

Extract the archive at the repository root.

In `src/eke/presentation/api/app.py`:

1. import `import_jobs_router`;
2. register it with:

```python
app.include_router(import_jobs_router)
```

In `tests/presentation/api/test_openapi_contract.py`, add:

```text
/imports/eurlex/jobs
/imports/eurlex/jobs/{job_uuid}
/imports/eurlex/jobs/{job_uuid}/run
```

Also expect these operation IDs:

```text
eurlex_import_jobs_create_import_job
eurlex_import_jobs_get_import_job
eurlex_import_jobs_run_import_job
```

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(presentation): add import jobs API
```
