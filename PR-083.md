# PR-083 — EUR-Lex Conditions and Exceptions

Extract this archive at the repository root, then run:

```cmd
apply_pr083.cmd
```

Commit:

```text
feat(eurlex): extract explicit rule conditions and exceptions
```
