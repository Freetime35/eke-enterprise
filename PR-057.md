# PR-057 — Stale Import Job Recovery

## Branch

```bash
git checkout main
git pull
git checkout -b feature/pr-057-stale-job-recovery
```

## OpenAPI additions

Add:

```python
"/imports/eurlex/jobs/{job_uuid}/recover-stale",
```

and:

```python
assert (
    "eurlex_import_jobs_recover_stale_import_job"
    in operation_ids
)
```

## Validation

```bash
python -m ruff check .
python -m mypy
python -m pytest
```

## Commit

```text
feat(application): add stale import job recovery
```
