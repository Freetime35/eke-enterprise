# PR-076 — EUR-Lex Legal Obligations Extraction

Extract this archive at the repository root, then run:

```cmd
apply_pr076.cmd
```

Commit:

```text
feat(eurlex): extract explicit English legal obligations
```
