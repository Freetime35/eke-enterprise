# PR-061 — Import Job Result Items Summary

Extract this archive at the repository root, then run:

```cmd
apply_pr061.cmd
```

Commit:

```text
feat(application): summarize import job result items
```
