# PR-054 — Import Job Operational Metrics

```bash
git checkout main
git pull
git checkout -b feature/pr-054-import-job-metrics
```

Extract the archive.

In `tests/presentation/api/test_openapi_contract.py`, add:

```text
/imports/eurlex/jobs/metrics
```

and:

```text
eurlex_import_jobs_get_import_job_metrics
```

Validate:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add import job operational metrics
```
