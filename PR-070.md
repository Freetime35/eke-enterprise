# PR-070 — EUR-Lex Consolidated Acts and Version Lineage

This v3 archive replaces the previous PR-070 model and can be applied to a
branch already modified by PR-070 v1 or v2.

Extract at the repository root, then run:

```cmd
apply_pr070.cmd
```

Commit:

```text
feat(eurlex): import consolidated act version lineage
```
