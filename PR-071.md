# PR-071 — EUR-Lex Corrigenda & Rectificatif Identifiers

Extract this archive at the repository root, then run:

```cmd
apply_pr071.cmd
```

Commit:

```text
feat(eurlex): import corrigenda identifiers and metadata
```
