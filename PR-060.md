# PR-060 — Import Job Result Items Inspection

Extract this archive at the repository root, then run:

```cmd
apply_pr060.cmd
```

Commit:

```text
feat(application): expose import job result items
```
