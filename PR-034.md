# PR-034 — Resource Provenance API

```bash
git checkout main
git pull
git checkout -b feature/pr-034-resource-provenance-api
```

Extract the archive at the repository root, then run:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add Resource Provenance API
```
