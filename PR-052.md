# PR-052 — Import Job Lineage

```bash
git checkout main
git pull
git checkout -b feature/pr-052-import-job-lineage
```

Extract the archive.

In `tests/presentation/api/test_openapi_contract.py`, add:

```text
/imports/eurlex/jobs/{job_uuid}/lineage
```

and:

```text
eurlex_import_jobs_get_import_job_lineage
```

Validate:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add import job lineage
```
