# PR-067 — EUR-Lex Financial Regulatory Family Detection

Extract this archive at the repository root, then run:

```cmd
apply_pr067.cmd
```

Commit:

```text
feat(eurlex): detect financial regulatory families
```
