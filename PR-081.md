# PR-081 — EUR-Lex Compliance Rule Extraction

Extract this archive at the repository root, then run:

```cmd
apply_pr081.cmd
```

Commit:

```text
feat(eurlex): derive compliance rules from requirements graph
```
