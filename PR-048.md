# PR-048 — Asynchronous Import Job Submission

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-048-async-import-job-submission
```

Extract the archive at the repository root.

In `tests/presentation/api/test_openapi_contract.py`, add:

```text
/imports/eurlex/jobs/{job_uuid}/submit
```

and expect:

```text
eurlex_import_jobs_submit_import_job
```

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(presentation): submit import jobs asynchronously
```
