# PR-056 — Stale Import Job Detection

```bash
git checkout main
git pull
git checkout -b feature/pr-056-stale-import-jobs
```

Extract the archive.

In `tests/presentation/api/test_openapi_contract.py`, add:

```text
/imports/eurlex/jobs/stale
```

and:

```text
eurlex_import_jobs_get_stale_import_jobs
```

Validate:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add stale import job detection
```
