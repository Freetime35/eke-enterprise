# PR-040 — EUR-Lex Resource Import Service

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-040-eurlex-resource-import
```

Extract the archive at the repository root.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(application): add EUR-Lex Resource import
```
