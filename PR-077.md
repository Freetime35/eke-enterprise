# PR-077 — EUR-Lex Legal Permissions Extraction

Extract this archive at the repository root, then run:

```cmd
apply_pr077.cmd
```

Commit:

```text
feat(eurlex): extract explicit English legal permissions
```
