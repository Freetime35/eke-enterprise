# PR-055 — Import Job Duration Statistics

```bash
git checkout main
git pull
git checkout -b feature/pr-055-import-job-durations
```

Extract the archive.

In `tests/presentation/api/test_openapi_contract.py`, add:

```text
/imports/eurlex/jobs/durations
```

and:

```text
eurlex_import_jobs_get_import_job_duration_statistics
```

Validate:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add import job duration statistics
```
