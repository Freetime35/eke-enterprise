# PR-079 — EUR-Lex Cross References and Internal References

Extract this archive at the repository root, then run:

```cmd
apply_pr079.cmd
```

Commit:

```text
feat(eurlex): extract and resolve internal references
```
