# PR-078 — EUR-Lex Legal Prohibitions Extraction

Extract this archive at the repository root, then run:

```cmd
apply_pr078.cmd
```

Commit:

```text
feat(eurlex): extract explicit English legal prohibitions
```
