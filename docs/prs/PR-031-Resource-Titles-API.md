# PR-031 — Resource Titles API

Adds ResourceTitleService, nested title endpoints, DTOs, mappers, error mappings,
tests, and ADR-0026.

Validation:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```
