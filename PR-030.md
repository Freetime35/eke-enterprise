# PR-030 — Resource Search & Pagination

## Apply

```bash
git checkout main
git pull
git checkout -b feature/pr-030-resource-search
```

Extract this archive at the repository root.

## Validate

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(application): add Resource search and pagination
```
