# PR-032 — Resource Versions API

```bash
git checkout main
git pull
git checkout -b feature/pr-032-resource-versions-api
```

Extract the archive at the repository root, then run:

```bash
python -m pytest
python -m ruff check .
python -m mypy
```

Commit:

```text
feat(application): add Resource Versions API
```
