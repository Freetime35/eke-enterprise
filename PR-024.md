# PR-024 — SQLAlchemy ResourceRepository

## Apply on branch

```bash
git checkout main
git pull
git checkout -b feature/pr-024-sqlalchemy-resource-repository
```

## Required pyproject change

Add `"SQLAlchemy>=2.0,<3.0"` to `[project].dependencies`.

## Validation

```bash
python -m pip install -e ".[dev]"
python -m pytest
python -m ruff check .
python -m mypy
```

## Commit

```text
feat(infrastructure): add SQLAlchemy ResourceRepository
```
